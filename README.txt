- open the project
- run the following commands to run the project
- open terminal or console of the opened IDE
- create virtual environment of pip
- run the commands
	- cd myapp
	- pip install django
	- python -m pip install Pillow
	- python manage.py runserver
- go to the URL on the terminal/console screen

#############################################
- admin screen can be accessing by going to the /admin page
admin credentials:
	admin
	123